<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Find a quiz</title>
  <link href="./styles.css" rel="stylesheet" type="text/css" />
</head>
<body>
  <div class="container">
    <h1>Quizzes</h1>
    <p>Find all available quizzes here</h1>
    <div class="quizzes">
      <% data['quizzes'].forEach(quiz => { %>
        <a href="./quiz?name=<%- quiz.slug %>"><button><%= quiz.title %></button></a>
      <% }) %>
    </div>
  </div>

  <div class="container">
    <h1>Your Token Balance: </h1>
    <h2><%= data['userLogin'].tokens %></h2>
    <% if (data['userLogin'].tokens >= 9) { %>
      <h1>Congrats! You unlocked the door code: UP UP DOWN LEFT RIGHT</h1>
    <% } %>
    <h1>Your current scores:</h1>
    <% if (data['userLogin'].aiComplete < 3) { %>
      <h2 style="color : red;">Artificial Intelligence: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Artificial Intelligence: <%= data['userLogin'].aiComplete %>/5 correct</h2>
    <% } %>

    <% if (data['userLogin'].cloudComplete < 3) { %>
      <h2 style="color : red;">Cloud Computing: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Cloud Computing: <%= data['userLogin'].cloudComplete %>/5 correct</h2>
    <% } %>

    <% if (data['userLogin'].dsComplete < 3) { %>
      <h2 style="color : red;">Data Science: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Data Science: <%= data['userLogin'].dsComplete %>/5 correct</h2>
    <% } %>

    <% if (data['userLogin'].engiComplete < 3) { %>
      <h2 style="color : red;">Engineering: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Engineering: <%= data['userLogin'].engiComplete %>/5 correct</h2>
    <% } %>

    <% if (data['userLogin'].quantumComplete < 3) { %>
      <h2 style="color : red;">Quantum: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Quantum: <%= data['userLogin'].quantumComplete %>/5 correct</h2>
    <% } %>

    <% if (data['userLogin'].securityComplete < 3) { %>
      <h2 style="color : red;">Security: Not Complete</h2>
    <% } else { %>
      <h2 style="color: green;">Security: <%= data['userLogin'].securityComplete %>/5 correct</h2>
    <% } %>
  </div>
  <div class="container" id="email">
    <p>Your email: <%= data['userLogin'].email %></p>
  </div>
  
  <script src="./login.js"></script>

</body>
</html>